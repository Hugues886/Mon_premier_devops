function activerMagie() {
            // On change la bordure de la carte e>
            document.getElementById('maCarte').st>

            // On modifie le texte du statut
            document.getElementById('statut').inn>
            document.getElementById('statut').sty>
            document.getElementById('horloge').in>


            // On affiche une fenêtre pop-up
            alert("Bienvenue dans le monde de Jav>
        }
        function changerMode() {
    // On récupère le corps de la page (le body)
    let corps = document.body;

    // Si la couleur est bleu foncé, on passe au >
    if (corps.style.backgroundColor === "rgb(44, >
        corps.style.backgroundColor = "#ecf0f1";
        corps.style.color = "#2c3e50";
    } else {
        corps.style.backgroundColor = "#2c3e50";
        corps.style.color = "white";
    }
}
